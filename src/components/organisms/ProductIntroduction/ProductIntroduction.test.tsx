import { render, screen } from '@/utils/testUtils'
import { ProductIntroduction } from './ProductIntroduction'

it('**********************', () => {
  const { container } = render(<ProductIntroduction />)

  // expect(container).toMatchSnapshot()
})
