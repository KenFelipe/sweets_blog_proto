import * as Styled from './{{pascalCase ComponentName}}.styled'

export type {{pascalCase ComponentName}}Props = {
  children: React.ReactNode
}

export const {{pascalCase ComponentName}} = ({ children }: {{pascalCase ComponentName}}Props) => {
  return (
    <Styled.Container>
      {children}
    </Styled.Container>
  )
}
